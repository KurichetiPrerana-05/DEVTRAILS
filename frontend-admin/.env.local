# .env.example — Admin Dashboard environment variables
# Copy to .env.local and fill in values before running

# Backend API base URL (Member 2's Express server)
VITE_API_BASE_URL=http://localhost:3001
